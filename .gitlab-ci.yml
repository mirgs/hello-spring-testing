default:
    image: openjdk:11-jdk

build-job:
    stage: build
    script:
        - ./gradlew assemble
    artifacts:
        when: always
        paths:
            - build/libs/*.jar
        reports:
            junit:
                - build/test-results/test/TEST-*.xml

test-job:
    stage: test
    script:
        - ./gradlew test
        

deploy-job:
    stage: deploy
    script:
        - echo "Deploy"
